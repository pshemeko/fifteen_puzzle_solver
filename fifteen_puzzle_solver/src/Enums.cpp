#include "Enums.h"
#include "pch.h"


/*
#pragma warning(3 : 4061) // enable missing enum value warning

#define SWITCH_LOGS(log) case Moves::log: return #log

const char *toString(Moves log)
{
    switch (log)
    {
        SWITCH_LOGS(Left);
        SWITCH_LOGS(Right);
        SWITCH_LOGS(Up);
        SWITCH_LOGS(Down);
    default:
        return "unknown log level";
    }
}


#define CASE_LOG(log, str) case Moves::log: return str

const char *toDescription(Moves log)
{
    switch (log)
    {
        CASE_LOG(Left, "L");
        CASE_LOG(Right, "R");
        CASE_LOG(Up, "U");
        CASE_LOG(Down, "D");
    default:
        return "unknown log level";
    }
}
*/
